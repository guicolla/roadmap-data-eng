name: 'spotify_analytics'
version: '1.0.0'
config-version: 2
profile: 'spotify_analytics'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]

seeds:
  spotify_analytics:
    +schema: raw

models:
  spotify_analytics:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
